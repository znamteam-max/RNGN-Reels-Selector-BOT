[project]
name = "rngn-reels-selector-bot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "google-api-python-client==2.179.0",
    "google-auth==2.40.3",
    "requests==2.32.4"
]

[tool.vercel]
entrypoint = "api.webhook:handler"
